#include <stdio.h>

/*
 * puts works pretty much like printf, it prints out the content given to it to stdout and appends a \n
 * don't forget to return 0 - this means the code ran successfully
 */
int main(void)
{
	char banner[]="Programming is like building a multilingual puzzle";
	puts(banner);
	return(0);
}
