#include <stdio.h>
/*
 * this time round we use printf, note it does not append a \n and you'll have to do that yourself.
 */
int main()
{
	char banner[]="with proper grammar, but the outcome is a piece of art,\n";
	printf("%s",banner);
	return 0;
}
